{% comment %} you can always turn off the speedupkernel{% endcomment %}
{%- assign speedupkernel = true -%}
{%- assign is_admin_interface = true -%}

{%- if speedupkernel -%}

	{%- if request.host contains 'myshopify.com' -%}
		{%- assign is_admin_interface = true -%}
	{%- else -%}
		{%- assign is_admin_interface = false -%}
	{%- endif -%}

	{%- if request.host contains 'shopifypreview.com' -%}
		{%- unless is_admin_interface -%}
			{%- assign is_admin_interface = true -%}
		{%- endunless -%}
	{%- endif -%}

	{%- if request.host contains 'gempages.net' -%}
		{%- unless is_admin_interface -%}
			{%- assign is_admin_interface = true -%}
		{%- endunless -%}
	{%- endif -%}	

	{%- if request.path == '/cart' -%}
		{%- unless is_admin_interface -%}
			{%- assign is_admin_interface = true -%}
		{%- endunless -%}
	{%- endif -%}

	{%- if request.path == '/checkout' -%}
		{%- unless is_admin_interface -%}
			{%- assign is_admin_interface = true -%}
		{%- endunless -%}
	{%- endif -%}

	{%- if request.path contains '/account' -%}
		{%- unless is_admin_interface -%}
			{%- assign is_admin_interface = true -%}
		{%- endunless -%}
	{%- endif -%}

	{%- if request.path contains '/apps/' -%}
		{%- unless is_admin_interface -%}
			{%- assign is_admin_interface = true -%}
		{%- endunless -%}
	{%- endif -%}

	{%- if request.path contains '/products/neck-decolletage-led' -%}
		{%- unless is_admin_interface -%}
			{%- assign is_admin_interface = true -%}
		{%- endunless -%}
	{%- endif -%}

	{%- if request.path contains '/products/q-urify-water-filter' -%}
		{%- unless is_admin_interface -%}
			{%- assign is_admin_interface = true -%}
		{%- endunless -%}
	{%- endif -%}

	{%- if request.path contains '/products/face-serum' -%}
		{%- unless is_admin_interface -%}
			{%- assign is_admin_interface = true -%}
		{%- endunless -%}
	{%- endif -%}

	{%- if request.path contains '/pages/qure-microinfusion-offer' -%}
		{%- unless is_admin_interface -%}
			{%- assign is_admin_interface = true -%}
		{%- endunless -%}
	{%- endif -%}

	{%- if request.path contains '/pages/exclusive-holiday-bundle-deal' -%}
		{%- unless is_admin_interface -%}
			{%- assign is_admin_interface = true -%}
		{%- endunless -%}
	{%- endif -%}

	{%- if request.path contains '/pages/microinfusion-safety-guide' -%}
		{%- unless is_admin_interface -%}
			{%- assign is_admin_interface = true -%}
		{%- endunless -%}
	{%- endif -%}

	{%- if request.path contains '/pages/q-urify-water-filter-offer' -%}
		{%- unless is_admin_interface -%}
			{%- assign is_admin_interface = true -%}
		{%- endunless -%}
	{%- endif -%}
{%- endif -%}